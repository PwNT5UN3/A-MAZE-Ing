[build-system]
requires = ["setuptools>=61", "numpy>=1.21", "wheel>=0.36"]
build-backend = "setuptools.build_meta"

[project]
version = "0.0.0"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = { "" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
